# Common

* Contains shared utilities, helpers, constants, exceptions, base classes, and reusable components used across the entire application.
* No business logic should live here — only cross-cutting concerns.